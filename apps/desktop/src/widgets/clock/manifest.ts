/**
 * The clock manifest — moved.
 *
 * Re-exported from its new home alongside the rest of the first-party widget
 * set. Kept here because modules outside this stage's ownership import it.
 */

export { CLOCK_MANIFEST } from '../../desktop/widgets/clock';
